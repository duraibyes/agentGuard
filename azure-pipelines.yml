trigger:
  - main
  - main-post

variables:
  appName: "AgentGaurd"
  azureSubscription: "b3c51356-7232-451a-8be9-7eb934a573d8"
  containerRegistry: "reciperegistry.azurecr.io"
  imageRepository: "AgentGaurd"
  dockerRegistryServiceConnection: "recipeRegistry-connection"
  tag: "$(Build.BuildId)"

pool:
  vmImage: "ubuntu-latest"

steps:
  - task: Docker@2
    displayName: "Build Docker image"
    inputs:
      command: build
      repository: $(imageRepository)
      dockerfile: '$(Build.SourcesDirectory)/.devcontainer/Dockerfile'
      containerRegistry: $(dockerRegistryServiceConnection)
      tags: |
        $(tag)
        latest

  - task: Docker@2
    displayName: "Push Docker image to ACR"
    inputs:
      command: push
      repository: $(imageRepository)
      containerRegistry: $(dockerRegistryServiceConnection)
      tags: |
        $(tag)
        latest

  - task: AzureWebAppContainer@1
    displayName: "Deploy container to App Service"
    inputs:
      azureSubscription: $(azureSubscription)
      appName: $(appName)
      containers: $(containerRegistry)/$(imageRepository):$(tag)