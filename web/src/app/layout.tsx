export const metadata = {
  title: "Agent Guard",
  description: "Agent Guard observability layer",
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  );
}
